# PRD：香港主体开户材料信息提取工具

## 1. 背景与目标

进件流程中，我们从客户提供的香港主体文件中人工抄录字段（企业名称、注册号、成立日期、代表人信息等），效率低且易出错。本工具通过解析上传的 PDF 文件，自动提取结构化字段，输出可直接用于填表/录入的结果，减少人工抄录。

**目标**：用户上传一组香港主体文件 → 工具自动识别并输出所需的全部字段 → 用户复核后即可使用。

## 2. 使用范围（In / Out of Scope）

**In scope**
- 解析三类香港文件：CI（Certificate of Incorporation 公司注册证书）、BR（Business Registration Certificate 商业登记证）、NNC1/NAR1（法团成立表格 / 周年申报表）。
- 提取企业信息与企业代表（董事/授权代表）个人信息。
- 支持文字层 PDF；对扫描件/图片型 PDF 走 OCR fallback（繁体中文 + 英文）。
- 本地运行，输出结构化结果（JSON + CSV），字段缺失时留空并标注来源。

**Out of scope（本期不做）**
- 证件正本（护照/身份证）真伪校验。
- 多主体批量并发（本期按单主体、单次一组文件处理即可）。
- 前端美化 / 云端部署（本期命令行或简单本地页面即可）。

## 3. 输入规格

提供多个文件上传窗口，按文件类型分区，避免类型误判：

| 窗口 | 文件类型 | 说明 | 主要提供字段 |
|---|---|---|---|
| 窗口 1 | **CI** | 公司注册证书 | 企业英文名称、注册号码、成立日期 |
| 窗口 2 | **BR** | 商业登记证 | 对外名称、注册地址、成立/生效日期、行业 |
| 窗口 3 | **NNC1 / NAR1** | 法团成立表格 / 周年申报表 | 注册地址、董事/代表个人信息、股东信息 |

**输入约束**
- 每个窗口接受 1 个或多个 PDF（同类型可多份，如新旧 NAR1）。
- 支持文字层 PDF 与扫描件 PDF。
- 单文件建议 ≤ 20MB；超出给出提示但不阻断。
- 文件命名不作硬性要求，但保留原文件名用于结果溯源。

## 4. 输出规格（字段清单）

输出分两组：**企业信息** 与 **企业代表**。标 `*` 为必填字段（缺失时高亮提示人工补录），未标为选填。

### 4.1 企业信息

| 字段 | 必填 | 主要来源 | 提取/推断说明 |
|---|---|---|---|
| 企业英文名称 | * | CI / NNC1 | 以 CI 上的法定英文名为准，通常以 LIMITED/LTD 结尾 |
| 企业对外名称 | | BR | 若 BR 上有商业名称（trading name）则取之，否则同英文名称 |
| 公司注册号码（CRN） | * | CI | 香港 CRN 通常为 7–8 位数字 |
| 成立日期 | * | CI | Date of Incorporation，统一格式化为 YYYY-MM-DD |
| 注册国家 | | 规则默认 | 香港文件默认 "Hong Kong / 中国香港"，允许人工修改 |
| 注册城市 | * | 规则默认 | 默认 "Hong Kong"，允许人工修改 |
| 注册详细地址 | * | NNC1/NAR1 / BR | Registered Office Address（注册办事处地址）完整地址 |
| 经营国家 | * | 规则默认 / BR | 默认 "Hong Kong"，若 BR 有经营地址则以其国家为准 |
| 经营详细地址 | * | BR / NAR1 | 经营/业务地址；若无独立经营地址，默认同注册地址并标注 |

### 4.2 企业代表（董事 / 授权代表）

代表信息主要来自 **NNC1/NAR1** 的董事及创办成员部分。若表内含多名董事，输出全部并允许用户勾选主代表。

| 字段 | 必填 | 主要来源 | 提取/推断说明 |
|---|---|---|---|
| 代表姓氏（中文） | * | NNC1/NAR1 | 中文姓氏，若文件无中文名则留空待补 |
| 名字（中文） | * | NNC1/NAR1 | 中文名字 |
| 英文姓氏 | * | NNC1/NAR1 | Surname / Family name |
| 英文名字 | * | NNC1/NAR1 | Given name |
| 职位 | * | NNC1/NAR1 | 如 Director / 董事，默认 "Director" 可改 |
| 性别 | * | NNC1/NAR1 或证件 | 表格常无性别，需从证件或人工补录，缺失则标注 |
| 国籍 | * | NNC1/NAR1 | 董事的国籍字段 |
| 居住国家 | * | NNC1/NAR1 | 从住址（Residential Address）推断国家 |
| 同国籍 | | 派生 | 布尔：居住国家 == 国籍 时自动为"是" |
| 居住地址 | * | NNC1/NAR1 | Residential Address 完整地址 |
| 证件签发国家 | * | 证件信息推断 | 从证件类型/号码推断，缺失则待人工补录 |
| 出生日期 | * | 证件 / NNC1 | 统一格式化为 YYYY-MM-DD |
| 证件信息 | * | 证件 / NNC1 | 证件类型 + 号码（如护照号 / 身份证号）；表格常做部分遮蔽，需标注 |

## 5. 字段处理与规则

- **日期归一化**：所有日期统一输出为 `YYYY-MM-DD`；识别到的原始格式（如 "15 March 2023"、"2023年3月15日"）保留在 `raw` 字段供复核。
- **默认值规则**：注册国家/城市、经营国家在香港主体下有合理默认值，但必须标记为"默认值(default)"而非"提取值(extracted)"，提醒用户确认。
- **派生字段**："同国籍"由居住国家与国籍比对派生；"经营地址"缺失时回退注册地址并标注 `inferred`。
- **多来源冲突**：同一字段被多份文件命中且值不一致时，按来源优先级取值（CRN/成立日期以 CI 为准；注册地址以最新 NAR1 为准），并在结果中记录冲突项。
- **缺失与遮蔽**：必填字段缺失或证件号被部分遮蔽（`****`）时，字段值留空，`status` 标为 `missing` 或 `masked`，供前端高亮。

## 6. 输出格式

每个字段以对象形式输出，携带来源与置信度，便于人工复核：

```json
{
  "enterprise": {
    "name_en":      { "value": "GWEN INTERNATIONAL LIMITED", "source": "CI",   "status": "extracted", "raw": "..." },
    "crn":          { "value": "12345678",                    "source": "CI",   "status": "extracted" },
    "incorp_date":  { "value": "2023-03-15",                  "source": "CI",   "status": "extracted", "raw": "15 March 2023" },
    "reg_country":  { "value": "Hong Kong",                   "source": "rule", "status": "default" },
    "reg_address":  { "value": "...",                         "source": "NAR1", "status": "extracted" }
  },
  "representatives": [
    {
      "surname_en":   { "value": "CHAN",  "source": "NNC1", "status": "extracted" },
      "given_en":     { "value": "TAI MAN", "source": "NNC1", "status": "extracted" },
      "position":     { "value": "Director", "source": "NNC1", "status": "extracted" },
      "gender":       { "value": "",       "source": "",     "status": "missing" },
      "id_info":      { "value": "P12****", "source": "NNC1", "status": "masked" }
    }
  ],
  "conflicts": [],
  "files": ["CI.pdf", "BR.pdf", "NAR1_2024.pdf"]
}
```

同时导出一份 **CSV**（UTF-8 BOM，Excel 直接打开不乱码），字段扁平化为列，缺失/遮蔽字段留空并在末列备注 `status`。

## 7. 技术方案建议

- **语言/环境**：Python 3.10+，本地运行，无需联网。
- **文字层提取**：`pdfplumber`（保留版面便于定位字段）。
- **OCR fallback**：文字层字符数低于阈值判定为扫描件，用 `pdf2image` + `pytesseract`，语言包 `chi_tra+eng`，DPI 300。
- **字段匹配**：正则 + 关键词锚定（按 CI/BR/NNC1/NAR1 各自版式分别写规则集），后续可升级为版面模板匹配。
- **交互**：本期可命令行（`python extract.py --ci ... --br ... --nnc1 ...`）或简单本地 Web 页面（三个上传区 + 结果表格 + 逐字段复核/编辑）。
- **依赖**：`pdfplumber`、`pdf2image`、`pytesseract`；系统层 `tesseract-ocr`（含 `chi_tra`）、`poppler`。

## 8. 前端页面（HTML）

本期提供一个本地运行的单页 HTML 界面，负责上传与结果复核，不含独立后端框架依赖（可由 Claude Code 用 Flask/FastAPI 起一个本地服务承接提取逻辑，前端通过 fetch 调用）。附件 `hk_extract_ui.html` 为可直接打开的交互原型，开发时以其为视觉与交互基准。

### 8.1 页面结构

**顶部**：标题「香港主体材料信息提取」+ 一行说明。

**上传区（三分栏）**：三个独立上传窗口横向排列，分别对应 CI、BR、NNC1/NAR1，每栏含：
- 类型标签（CI / BR / NNC1·NAR1）与中文名称；
- 一行用途说明（该类型提供哪些字段）；
- 拖拽/点击上传区，接受 1 或多个 PDF；
- 已上传文件列表，显示文件名、大小，可单独删除。

**操作条**：说明文案（扫描件将走 OCR）+「清空」+「开始提取」按钮。

**结果区**：
- 图例：已提取 / 默认值 / 遮蔽 / 缺失待补 四种状态色标；
- **企业信息**表格：字段名（必填带 `*`）｜可编辑值｜状态徽章｜来源文件；
- **企业代表**：每名董事一张卡片，卡片头显示姓名 +「设为主代表」单选，卡片内为同结构字段表格；多名董事时多卡片并列；
- 底部导出条：「导出 JSON」「导出 CSV」，并提示必填缺失/遮蔽字段需人工补录后再导出。

### 8.2 交互要求

- **就地编辑**：每个字段值为可编辑输入框，用户可直接修正提取结果；缺失（`missing`）字段输入框红底高亮并显示「待人工补录」占位符。
- **状态可视**：每字段右侧徽章标注 `extracted / default / masked / missing`，颜色区分；来源列显示命中的文件（如 CI、NAR1、rule、派生）。
- **主代表选择**：多董事时以单选按钮指定主代表，默认选第一位。
- **导出**：JSON 保留每字段 `value / source / status / raw`；CSV 扁平化为列（UTF-8 BOM，Excel 直接打开不乱码），末列备注状态。
- **反馈**：无文件时点击「开始提取」给出提示；提取中显示 loading（OCR 较慢时尤其需要）。

### 8.3 视觉规范

- 定位为内部合规工具，风格克制、以表格与文档感为主，非营销页；
- 字段状态用色：已提取（绿）、默认值（蓝）、遮蔽（橙）、缺失（红），与图例一致；
- 响应式：窄屏时三栏上传区堆叠为单列，结果表隐藏次要列（来源/文件）。

## 9. 验收标准

1. 上传标准文字层 CI/BR/NNC1（或 NAR1）后，企业信息全部必填字段正确提取（准确率 ≥ 95%）。
2. 扫描件 PDF 能走 OCR fallback 完成提取，注册号、日期等纯字段准确率 ≥ 90%，地址允许人工复核。
3. 所有日期统一输出 `YYYY-MM-DD`。
4. 缺失/遮蔽的必填字段被明确标注（`missing` / `masked`），不静默留空。
5. 同时产出 JSON 与 CSV，CSV 用 Excel 打开无乱码。
6. 多名董事时全部输出，且可选定主代表。

## 10. 待确认项（交给开发前请补充）

1. **证件字段来源**：性别、证件签发国、出生日期、完整证件号在 NNC1/NAR1 中常被遮蔽或缺失——是否会额外上传护照/身份证扫描件？若是，需增加"证件"上传窗口并纳入 OCR。
2. **企业对外名称定义**：是指 BR 上的商业名称（trading name），还是中文公司名称？
3. **主代表选取规则**：多董事时默认取第一位，还是需人工指定？
4. **交互形态**：命令行即可，还是需要一个本地网页界面？
5. **真实样本**：提供 1–2 份各类型脱敏样本用于校准正则/OCR，可显著提升首版准确率。
